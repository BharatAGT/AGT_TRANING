 // 🔹 Copy Delivery from Header to Line
    [EventSubscriber(ObjectType::Table, Database::"Sales Line", 'OnAfterValidateEvent', 'No.', false, false)]
    local procedure CopyDeliveryToLine(var Rec: Record "Sales Line")
    var
        SalesHeader: Record "Sales Header";
    begin
        if SalesHeader.Get(Rec."Document Type", Rec."Document No.") then begin
            Rec."Delivery" := SalesHeader."Delivery";

        end;
    end;


    // // 🔹 Prevent editing after Release
    // [EventSubscriber(ObjectType::Table, Database::"Sales Header", 'OnBeforeValidateEvent', 'Delivery', false, false)]
    // local procedure PreventEditAfterRelease(
    //     var Rec: Record "Sales Header";
    //     xRec: Record "Sales Header")
    // begin
    //     if Rec.Status = Rec.Status::Released then
    //         Error('You cannot modify Delivery after the order is released.');
    // end;